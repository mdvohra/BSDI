# Unified FastAPI backend (GeoAI). Used by the SPA for GET /api/ui-config and API calls.
# Copy to frontend/.env.local for local development.

VITE_API_BASE_URL=http://localhost:8000

# Legacy axios base URL (existing App.jsx) — align with backend port when used:
# VITE_APP_API_BASE_URL_DEV=http://localhost:8000
